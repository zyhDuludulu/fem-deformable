#include <vector>
#include <fmt/core.h>

class Simulator {
public:
  Simulator();
  // ~Simulator();
  void step();
  void reset();
private:
  // std::vector<glm::vec2> x;
  // std::vector<glm::vec2> v;
  // std::vector<glm::vec2> f;
  // std::vector<glm::vec2> A;
};